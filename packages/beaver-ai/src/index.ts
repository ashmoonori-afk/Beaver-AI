// beaver-ai package public surface.
//
// The library facade — `Beaver` — and supporting types are defined in
// ./api.ts. The CLI (packages/cli) imports `Beaver` from this package; the
// programmatic library API and the CLI go through the exact same code path.

export * from './api.js';
